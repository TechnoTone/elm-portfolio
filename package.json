{
  "private": true,
  "scripts": {
    "start": "webpack --watch --mode development --env.mode development --env.builddir build",
    "clean": "rimraf node_modules elm-stuff build"
  },
  "dependencies": {
    "elm": "^0.19.0-no-deps"
  },
  "devDependencies": {
    "express": "^4.17.1",
    "rimraf": "^3.0.0",
    "webpack": "^4.39.2",
    "webpack-cli": "^3.3.7",
    "webpack-livereload-plugin": "^2.2.0"
  }
}
